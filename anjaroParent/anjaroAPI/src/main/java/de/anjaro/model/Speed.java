package de.anjaro.model;

/**
 * The Enum Speed.
 */
public enum Speed {

	/** The speed0. */
	speed0,/** The speed1. */
speed1,/** The speed2. */
speed2,/** The speed3. */
speed3,/** The speed4. */
speed4,/** The speed5. */
speed5,/** The speed6. */
speed6,/** The speed7. */
speed7,/** The speed8. */
speed8,/** The speed9. */
speed9,/** The speed10. */
speed10;

}
